modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

run = "npm run dev"

[[ports]]
localPort = 3000
externalPort = 80

[deployment]
run = ["sh", "-c", "npm run build && npm run preview"]
deploymentTarget = "cloudrun"
